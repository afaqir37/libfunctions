#include <stdio.h>

int main(void)
{
    int a;
    scanf("%d", &a);
    int b;
    scanf("%d", &b);

    int p[b][2];

    for (int i = 0; i < b; i++)
    {
        for (int j = 0; j < 2; j++)
            scanf("%d", &p[i][j]);
    }
    int c = 1;
    int j = 0;
    
    for (int j =0; j < b; j++)
    {
        
    }

   
}